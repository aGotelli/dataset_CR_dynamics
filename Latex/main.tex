\documentclass[14pt, a4paper]{article}
\usepackage[a4paper, margin=1in]{geometry}
\usepackage{graphicx} % Required for inserting images
\usepackage{siunitx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{pdflscape}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{parskip}




% NSD: Title must be <= 110 characters, no marketing words (comprehensive, 
% novel, first, AI-ready, open), no colons or parentheses, no acronyms.
%
% Current: 52 chars (OK length), but "Comprehensive" is discouraged.
% Suggested alternatives (pick one):
%   "Multi-sensor dataset of a tendon-driven continuum robot in dynamic motion" (72 chars)
%   "Multi-sensor dataset of tendon-driven continuum robot quasi-static and dynamic motion" (86 chars)
%
\title{Multi-sensor dataset of a tendon-driven continuum robot in dynamic motion}
\author{Andrea Gotelli, Tongjia Zheng, Angela Peloso, Spencer Teetaert, \\Puspita Dewi, Chengnan Shentu, Jessica Burgner-Kahrs}


\newcommand{\ie}{\textit{i.e.}}
\newcommand{\eg}{\textit{e.g.}}

%	Set the bibliography once and for all
\bibliographystyle{ieeetr}

\begin{document}
\maketitle


\begin{abstract}
% NSD: max 170 words. Should describe the data and how it may be used.
% Should NOT make claims regarding new scientific findings.
% Do not include URLs or sub-headings.
%
\textbf{TO CHANGE}
    This data descriptor presents a multi-sensor dataset on the quasi-static and dynamic motion of a tendon-driven continuum robot, including scenarios with and without contact with objects in the workspace.
    The robot is instrumented with a motion capture system, fiber Bragg grating sensors, cable tension gauges, motor encoders, a base force-torque sensor, inertial measurement units, and a contact force-torque sensor.
    The dataset covers planar, circular, and Lissajous trajectories at multiple speeds, organized in four batches: quasi-static motions, dynamic motions, dynamic motions with body-mounted inertial measurement units, and dynamic motions with contact.
    Both raw sensor recordings and processed data (filtered, time-aligned, uniformly resampled) are provided.
    The dataset enables benchmarking of dynamics models, training of data-driven methods, study of contact interactions, and sensor fusion research for continuum robots.
\end{abstract}


\section{Background \& Summary}
%
% NSD GUIDANCE: Provide an overview of the dataset, the motivation for creating 
% it, and its potential reuse value. Cite related datasets. Do NOT include 
% subjective claims on novelty, impact, or utility. Do NOT write a full 
% literature review — keep the CR introduction to ~1 short paragraph.
%
% RECOMMENDED STRUCTURE (3 paragraphs + 1 dataset-summary paragraph):
%
% PARAGRAPH 1 — Context (SHORT, ~5-6 sentences max)
%   - Define continuum robots in one sentence.
%   - State they are studied for dynamics modeling, ML, and contact/interaction.
%   - Cite 2-3 key references for each (Boyer, Renda for dynamics; 
%     a ML ref; Rao, Matthew for contact).
%   - Do NOT explain what a CR is at tutorial level — this is not a survey.
%     The NSD audience is broad but the paper targets robotics researchers 
%     who already know what a CR is.
%   => DELETE: the figures of rigid-vs-continuum, bio-inspiration paragraph,
%     history back to 1960s, infinite DoF explanation. These belong in a 
%     survey or journal paper, not a data descriptor.
%
% PARAGRAPH 2 — Gap / Motivation (~4-5 sentences)
%   - State the problem: experimental validation in CR research relies on 
%     ad-hoc prototypes with lab-specific sensors; published datasets are 
%     scarce.
%   - Cite the Grassmann CTR dataset as a related effort (different robot type).
%   - Note that no existing dataset combines kinematics AND force measurements 
%     for a tendon-driven CR in both quasi-static and dynamic regimes.
%
% PARAGRAPH 3 — Dataset overview (~6-8 sentences)
%   - State what this dataset contains: a tendon-driven CR with 4 cables, 
%     instrumented with OptiTrack, FBGS, Mark10 tension sensors, ATI-FT, 
%     IMUs, and Resense FT for contact.
%   - Describe the robot briefly (single-segment, 4 tendons, ~0.48 m length).
%   - Describe the experiment batches: quasi-static, dynamic, with IMU, 
%     with contact.
%   - Describe trajectory types: planar, circular, Lissajous.
%   - State the data includes both raw and processed versions.
%
% PARAGRAPH 4 — Reuse value (~3-4 sentences)
%   - List concrete reuse scenarios: benchmarking dynamics models, training 
%     data-driven approaches, studying contact/interaction, sensor fusion 
%     research, parameter identification.
%   - Keep factual — do not say "this will enable" or "this is the first".
%
%% ---- DRAFT TEXT (to be refined) ----

Continuum robots are manipulators whose body undergoes continuous elastic deformation, actuated by tendons, pneumatics, or other distributed mechanisms~\cite{Robinson1999, BurgnerKahrs2015}.
Their dynamics have been studied through various modeling approaches \cite{Renda2016,Till2019, Boyer2021, Renda2024}, and there is growing interest in applying machine learning methods, in both statics \cite{Bensch2024} and dynamics \cite{Du2026}, as well as studying their contact and interaction with the environment \cite{Rao2024} with new studies addressing the contact kinematics with differentiable properties \cite{Mathew2026}.
%
However, experimental validation in continuum robotics typically relies on ad-hoc prototypes instrumented with lab-specific sensors, and the resulting data is rarely made publicly available.
A related effort produced a dataset for concentric tube robots~\cite{Grassmann2022}, but no publicly available dataset currently combines kinematics and force measurements for a tendon-driven continuum robot undergoing both quasi-static and dynamic motions, including contact with objects in the workspace.
%
This limited availability of open datasets prevents reproducibility of results and the comparison of competing modeling, control, and learning approaches on common ground.


% --- dataset overview ---
This data descriptor presents a multi-sensor dataset recorded on a single-segment tendon-driven continuum robot.
%
Kinematic measurements are provided by an OptiTrack motion capture system (disk poses), FBGS (distributed strain and shape), motor encoders (tendon displacements), and IMUs (angular velocities and linear accelerations). 
Force measurements are provided by Mark10 gauges (cable tensions), an ATI force-torque sensor (base wrench), and a Resense force-torque sensor (contact forces).
%
The dataset is organized in four subsets:
(i) quasi-static motions,
(ii) dynamic motions,
(iii) dynamic motions with IMUs mounted on the robot body,
and (iv) dynamic motions with contact with an object in the workspace.
Trajectories for dynamic motions include planar sweeps along x, y, and xy, star patterns, circular paths, and Lissajous figures, each executed with two maximum angular velocities for the actuators: slow ($1$ \si{\radian\per\second}) and fast ($3$ \si{\radian\per\second}).
Both raw sensor recordings and processed (filtered, resampled, time-aligned) data are provided.

% --- reuse value ---
The dataset is intended for benchmarking dynamics models against experimental data, training and evaluating data-driven methods, studying contact and interaction forces, investigating sensor fusion approaches, and performing parameter identification.
Because the robot is built from commercially available components and the design is fully documented, the dataset can also serve as a reference for groups building similar platforms.

In total, the dataset comprises N experiments totaling approximately X hours of multi-sensor recording (Y GB of raw data, Z GB processed).

\section{Methods}

In this section, we define the methodology used to create the presented dataset, starting from the robot design and components to the specifics of the sensors.
Finally, we describe how the data is collected giving a general idea of the data collection pipeline, the programming language and the use of CPU wall-time timestamps.  



\subsection{Design}
%% TODO (Puspita):
%% This subsection must contain enough detail for another lab to replicate the 
%% setup. NSD requires: "full descriptions of the experimental design".
%%
%% REQUIRED CONTENT:
%%   1. Robot backbone: material, diameter (2*Rc = 4 mm from parameters file), 
%%      length (0.48 m), supplier/part number if commercial.
%%   2. Disk design: number of disks (10?), spacing along arc-length 
%%      (0, 0.12, 0.24, 0.36, 0.48 m), material, how they are attached, 
%%      hole pattern for cable routing and sensor mounting.
%%   3. Cable routing: 4 tendons, routing distance from backbone center 
%%      (d = 37.5 mm), no pulleys.
%%   4. Base design: how it is mounted, why pulleys were avoided, how this 
%%      affects the cable tension measurement, how the ATI-FT is integrated.
%%   5. Constraints from Mark10 sensors: physical size, mounting requirements, 
%%      how they constrain the base design. State explicitly how the setup 
%%      can be modified if the user does not have Mark10 sensors (e.g., 
%%      replace with other load cells or passive pulleys).
%%   6. A figure showing the assembled robot with labeled components.
%%   7. A table or list of all purchased components with part numbers, 
%%      suppliers, and approximate cost.


\subsection{Sensors}
%% NSD requires: for each sensor, full technical specifications at a level 
%% that allows the reader to understand the measurement capabilities and 
%% limitations. Include: model, manufacturer, country, measurement range, 
%% resolution, accuracy, sampling rate, interface/protocol, and any relevant 
%% datasheet specifications.
%%
%% Andrea: Add a summary table at the end of this subsection with one 
%% row per sensor and columns: Sensor | Model | Manufacturer | Quantity measured | 
%% Range | Resolution/Accuracy | Sampling rate (Hz). 
%%

Different sensors have been mounted on the robot to capture different aspect of the motion, being it kinematics like position, angular velocities and linear accelerations, shape of the robot and pulled lenght of the cables, or force measures like tendon tensions, forces and torques at the base and at the contact with an object in the environment.
%
In this section, we describe the characteristics of each sensor in terms of frequency readings, range of outputs, and a summary of their specifics available on their datasheet.
\subsubsection{Mark 10}
%% TODO (Tongjia): model number, measurement range (N), resolution, 
%% accuracy class, sampling rate, communication interface (USB/serial), 
%% how the sensor is mounted in-line with the cable, zeroing procedure.

% For the cable tension, we used force gauge sensors from MRM Metrology inc. model M3-100 Mark-10 Digital Force Gauge 500 N. 

% Cable tension was measured using Mark-10 M3-5 digital force gauges
% (Mark-10 Corporation, Copiague, NY, USA;
% \url{https://mrmmetrology.com/product/m3-5-mark-10-digital-force-gauge/}),
% with a full-scale capacity of \SI{5}{lbf} ($\approx\SI{22.2}{\newton}$),
% a resolution of \SI{0.005}{lbf} ($\approx\SI{0.02}{\newton}$),
% and a manufacturer-specified accuracy of $\pm 0.3\%$ of full scale.
% units were calibrated by MRM~Metrology Inc.\ (Brampton, ON, Canada; ISO~17025 accredited), with an expanded uncertainty of $\pm\SI{0.007}{lbf}$ ($k=2$, 95\% coverage), traceable to SI through NIST and NRC.
% Each gauge was interfaced to the acquisition computer via a serial connection
% at \SI{115200}{baud} and polled continuously using a request--response protocol.
% The effective sampling rate, limited by the serial round-trip latency, was
% approximately \SI{335}{\hertz} per gauge.
% % TODO: fill in typical rate from your logs — the script prints it at the end.
% The output was recorded directly in newtons.


Cable tension was measured using Mark-10 M3-5 digital force gauges
(Mark-10 Corporation, Copiague, NY, USA;
\url{https://mrmmetrology.com/product/m3-5-mark-10-digital-force-gauge/}),
with a full-scale capacity of \SI{5}{lbf} ($\approx$\,\SI{22.2}{\newton}),
a resolution of \SI{0.005}{lbf} ($\approx$\,\SI{0.02}{\newton}),
and a manufacturer-specified accuracy of $\pm\,0.3\%$ of full scale.
All units were calibrated by an ISO~17025 accredited laboratory
with an expanded uncertainty of $\pm$\,\SI{0.007}{lbf} ($k{=}2$, 95\% coverage).
Each gauge was interfaced to the acquisition computer via a serial connection
at \SI{115200}{baud} and polled continuously using a request--response protocol.
The effective sampling rate, limited by the serial round-trip latency, was
approximately \SI{335}{\hertz} per gauge.
The output was recorded directly in newtons.
A dedicated Python script (one instance per gauge) opens the serial port via \texttt{pyserial},
sends an ASCII query command, parses the returned numeric string, and timestamps each reading with the system wall clock (\texttt{time.time()}).
All samples are buffered in a pre-allocated NumPy array and written to a CSV file at the end
of the recording.



% \subsubsection{FBGS}



% Distributed strain sensing along the robot backbone was performed using an FBGS multicore fiber (MCF)
% shape sensing system (FBGS International, Geel, Belgium; \url{https://www.fbgs.com}).
% The sensing fiber (ID: F13421-0007-003) is a four-core MCF with an outer diameter of \SI{250}{\micro\meter},
% featuring draw-tower gratings (DTGs) inscribed during the fiber drawing process.
% Each core contains 26 FBG gratings uniformly spaced at \SI{20}{\milli\meter} intervals,
% yielding an active sensing length of \SI{500}{\milli\meter}.
% The individual grating wavelengths span approximately \SIrange{1512}{1587}{\nano\meter} within the C-band,
% with a minimum inter-grating spacing of \SI{0.8}{\nano\meter}.
% The core-to-center distance is \SI{36.9}{\micro\meter} and the gauge factor is
% ${0.777}${$\mu\varepsilon^{-1}$}.
% Three of the four cores are used for shape reconstruction (core~2 omitted),
% enabling the decoupling of bending curvature, bending direction, and temperature along the fiber length.


% The fiber was interrogated by an FBG-Scan~904 spectrometer-based interrogator (FBGS),
% which features four optical channels over a \SIrange{1510}{1590}{\nano\meter} wavelength range
% with LC/APC connectors.
% The interrogator provides a wavelength precision of \SI{0.3}{\pico\meter} ($1\sigma$),
% a wavelength linearity of \SI{3}{\pico\meter},
% an absolute wavelength accuracy of \SI{10}{\pico\meter},
% and a dynamic range exceeding \SI{30}{\decibel}.
% Raw spectral data were processed by the ILLumiSense Shape~CORE software (v1.1, FBGS),
% which runs as a background process on the acquisition computer, receives spectra from the
% interrogator via USB, performs peak detection, and computes curvature, bending angle,
% three-dimensional shape, and temperature at each grating location.
% The reconstructed shape is output as three-dimensional Cartesian coordinates
% with \SI{1}{\milli\meter} arc-length resolution.
% With shape reconstruction enabled, the maximum achievable output rate of the Shape~CORE
% software is \SI{100}{\hertz}, which was used throughout all experiments.

% Shape~CORE streams its output over a TCP connection on the local machine (port~50012).
% A custom C++ client connects to this stream, receives curvature, bending angle, and
% shape arrays at each time step, timestamps each sample using the system high-resolution
% clock (\texttt{std::chrono::high\_resolution\_clock}), and stores all data in memory.
% At the end of the recording, the buffered data is written to a CSV file.
% In this work, we only account for the reconstructed shape, while the curvature and bending angle can be used as detailed in \cite{Moore2012}.

% Prior to measurements, the fiber was calibrated following the manufacturer's two-step procedure:
% a straight calibration to establish the reference wavelengths in the absence of bending strain,
% and a planar calibration to determine the reference twist of the fiber.

% The manufacturer specifies the position of the last grating with a tolerance of $\pm$\,\SI{5}{\milli\meter}.
% To determine this position with millimetre-level accuracy, a hot-wire contact was applied along the
% fibre starting from the distal end and advanced towards the base.
% Because the interrogator reports strain and temperature at each grating location,
% the last grating that registered a change in temperature upon contact was identified as the
% distal sensing point.
% This procedure yielded the arc-length coordinate of the last measured grating to within
% ${\sim}$\,\SI{1}{\milli\meter}, providing an accurate reference for shape reconstruction along the robot backbone.
% Once the distal grating was located, it was aligned with the tip disk of the continuum robot.
% The fibre was then fixed in place at both the base and the tip disk, ensuring a stable and repeatable
% routing along the backbone for all subsequent experiments.


\subsubsection{FBGS}
\label{sec:fbgs}


Distributed strain along the robot backbone is measured with an FBGS four-core multicore fibre
(MCF, ID: F13421-0007-003; FBGS International, Belgium) featuring 26 draw-tower gratings per
core at \SI{20}{\milli\meter} spacing, for an active sensing length of \SI{500}{\milli\meter}.
Three cores are used for shape reconstruction (core~2 omitted), enabling independent estimation
of bending curvature, bending direction, and temperature at each grating.

The fibre is interrogated by an FBG-Scan~904 spectrometer (\SIrange{1510}{1590}{\nano\meter},
4 channels, wavelength precision \SI{0.3}{\pico\meter} $1\sigma$).
ILLumiSense Shape~CORE~v1.1 performs peak detection and outputs the reconstructed 3-D shape
at \SI{1}{\milli\meter} arc-length resolution and \SI{100}{\hertz}.
A C++ client receives the TCP stream (port~50012), timestamps each sample with
\texttt{std::chrono::high\_resolution\_clock}, and writes the buffered data to CSV after
the recording.

Prior to use, the fibre was calibrated straight.
The distal grating was located to $\sim$\,\SI{1}{\milli\meter} via a hot-wire contact procedure and aligned with the tip disk; the fibre is fixed at the base and tip.
A residual angular offset between the fibre and robot bending planes is corrected
in post-processing using the initial planar calibration sweeps
(see Section~\ref{sec:kin_cross_val}).


\subsubsection{ATI-FT}
%% TODO (Tongjia): model (e.g., ATI Mini45), calibration range (Fx,Fy,Fz 
%% and Tx,Ty,Tz), resolution, sampling rate, DAQ interface, how it is 
%% mounted at the robot base, bias/tare procedure before each experiment.
% Tongjia: Add specifics for the ATI-FT sensor


% External force and torque measurements at the robot base were acquired using an ATI Mini40
% six-axis force/torque sensor (calibration SI-40-2; ATI Industrial Automation, Apex, NC, USA;
% \url{https://www.ati-ia.com/products/ft/ft_models.aspx?id=mini40}).
% The Mini40 is a monolithic transducer machined from hardened stainless steel via electrical
% discharge machining (EDM), instrumented with silicon strain gages that provide a signal-to-noise
% ratio approximately 75 times higher than conventional foil gages.
% The transducer has a diameter of \SI{40}{\milli\meter}, a height of \SI{12.2}{\milli\meter},
% and a mass of \SI{49.9}{\gram}.
% With the SI-40-2 calibration, the sensing ranges are
% $\pm\SI{40}{\newton}$ in $F_x$ and $F_y$,
% $\pm\SI{120}{\newton}$ in $F_z$,
% and $\pm\SI{2}{\newton\meter}$ in $T_x$, $T_y$, and $T_z$.
% % TODO: verify resolution values from your calibration certificate, e.g.:
% % Resolution: Fx,Fy = 1/XX N; Fz = 1/XX N; Tx,Ty,Tz = 1/XXXX Nm
% Single-axis overload ratings are $\pm\SI{810}{\newton}$ ($F_{xy}$) and
% $\pm\SI{2400}{\newton}$ ($F_z$), and the resonant frequencies are
% \SI{3200}{\hertz} ($F_x$, $F_y$, $T_z$) and \SI{4900}{\hertz} ($F_z$, $T_x$, $T_y$).
% The transducer was connected to a \texttt{DAQ F/T} interface and sampled at \SI{200}{\hertz}. 
External force and torque measurements at the robot base were acquired using an ATI Mini40
six-axis force/torque sensor (calibration SI-40-2; ATI Industrial Automation, Apex, NC, USA;
\url{https://www.ati-ia.com/products/ft/ft_models.aspx?id=mini40}).
The Mini40 is a monolithic transducer machined from hardened stainless steel via electrical
discharge machining (EDM), instrumented with silicon strain gages that provide a signal-to-noise
ratio approximately 75 times higher than conventional foil gages.
The transducer has a diameter of \SI{40}{\milli\meter}, a height of \SI{12.2}{\milli\meter},
and a mass of \SI{49.9}{\gram}.
With the SI-40-2 calibration, the sensing ranges are
$\pm\SI{40}{\newton}$ in $F_x$ and $F_y$,
$\pm\SI{120}{\newton}$ in $F_z$,
and $\pm\SI{2}{\newton\meter}$ in $T_x$, $T_y$, and $T_z$.
% TODO: verify resolution values from your calibration certificate
Single-axis overload ratings are $\pm\SI{810}{\newton}$ ($F_{xy}$) and
$\pm\SI{2400}{\newton}$ ($F_z$), and the resonant frequencies are
\SI{3200}{\hertz} ($F_x$, $F_y$, $T_z$) and \SI{4900}{\hertz} ($F_z$, $T_x$, $T_y$).
The transducer was connected in the DAQ~F/T configuration, with the six analog voltage channels
($\pm\SI{10}{\volt}$ range) digitized by a National Instruments data acquisition card
using the \texttt{nidaqmx} Python driver.
The hardware sampling clock was set to \SI{1000}{\hertz}; every five consecutive samples
per channel were averaged to yield an effective wrench output rate of approximately \SI{200}{\hertz}.
A $6\times6$ calibration matrix provided by ATI was applied in software to convert the
bias-subtracted voltage readings into calibrated force and torque values
($F_x$, $F_y$, $F_z$, $T_x$, $T_y$, $T_z$).
%The bias was determined at rest by averaging ten initial samples per channel prior to each acquisition session.

\subsubsection{Gyroscopes}
%% TODO (Spencer): IMU model, accelerometer and gyroscope ranges, resolution, sampling rate, 
%% communication interface (SPI/I2C), number of units and their placement 
%% on the robot body (which disks), mass of each unit.
% Spencer: Add specifics for the Gyroscopes

\textbf{TODO}
% {\color{blue}

% Four ISM330DHCX 6DoF IMU (SparkFun Electronics, Niwot, CO) units are used in this dataset. 

% }

% {\color{red}
% Missing / undetermined info: 
% \begin{itemize}
%     \item Position on body 
%     \item 
% \end{itemize}
% }

\subsubsection{Actuators}
%% TODO: fill in the control frequency, spool radius (0.02 m from code), 
%% spool material, motor encoder resolution.
% The tendons are actuated by four brushless DC motors (Cybergear, Xiaomi, China) with custom 3D-printed tendon spools of radius \SI{20}{\milli\metre}. 
% They are position controlled at XX \SI{}{\hertz} to track desired tendon displacement.


The tendons are actuated by four brushless DC motors (Cybergear, Xiaomi, China) with custom 3D-printed tendon spools of winding radius \SI{20}{\milli\metre}, i.e., the radius at which the tendon cable is coiled around the spool. 
Each motor is a quasi-direct-drive (QDD) module integrating a three-phase, 28-pole frameless torque motor with a 7.75:1 planetary gearbox, a 14-bit single-turn absolute encoder, and onboard FOC electronics, in a compact package of \SI{80.5}{\milli\metre} diameter and \SI{36.5}{\milli\metre} height (\SI{317}{\gram}). 
The motors are powered at \SI{24}{\volt} DC and provide a continuous output torque of 
\SI{4}{\newton\metre} and a peak torque of \SI{12}{\newton\metre}. 
Communication with the host PC is handled via a CAN 2.0 bus at \SI{1}{\mega\bit\per\second}. 
The four motors are operated as two antagonistic pairs (motors 1\&3 and 2\&4), each pair controlling one planar degree of freedom of the robot tip. 
They are operated in a closed-loop position control scheme, where target shaft angles 
$\theta^*$ are commanded directly over CAN, with shaft angle, velocity, and output torque 
returned as real-time feedback at each control cycle. 
Angles are computed relative to the home position, which is initialized as the reading angle at the motor startup.
The resulting tendon displacement $\Delta l = r_s \, \theta^*$, where $r_s = \SI{20}{\milli\metre}$ is the spool winding radius, is a geometric byproduct of the motor rotation.
The CAN bus is accessed from the host PC through a USB--CAN adapter (Canable / \texttt{candle} interface) using the \texttt{python-can} library.
A custom Python script, built on the open-source Cybergear driver~\cite{CybergearDocs}, instantiates four \texttt{CANMotorController} objects, sets each motor to position mode, and commands target angles in a sequential loop.
At each control cycle the script reads back the shaft angle, velocity, and torque from every motor and timestamps the sample with the system wall clock (\texttt{time.time()}).
The feedback data is buffered in memory and saved to a single CSV file at the end of the recording.

\subsubsection{Motion Capture System}
%% TODO (Spencer): OptiTrack system model, number of cameras, capture 
%% volume, marker type and size, number of rigid bodies tracked (5 disks 
%% + any objects), sampling rate, reported positional accuracy, software 
%% version (Motive), how rigid-body frames are defined relative to the 
%% robot body frame.
% Spencer: Add specifics for the Mocap

The shape of the robot backbone and the pose of objects in contact with it are measured using an optical motion capture system (OptiTrack, NaturalPoint, USA) composed of five PrimeX13 and two PrimeX13W cameras. 
The PrimeX13 cameras feature a 1.3\,MP sensor ($1280 \times 1024$\,px), a native frame rate of \SI{240}{\hertz}, a field of view of \ang{56} $\times$ \ang{46}, and a 3D positional accuracy of 
$\pm$\SI{0.15}{\milli\metre}. 
The two PrimeX13W cameras share the same sensor and frame rate but provide a wider field of view of \ang{82} $\times$ \ang{70}, suited to ensure full coverage of the compact workspace, at a slightly reduced accuracy of $\pm$\SI{0.30}{\milli\metre}. 
All cameras use \SI{850}{\nano\metre} infrared illumination and communicate via GigE/PoE. 
Five rigid bodies composed of passive retroreflective markers ($\approx$\SI{6}{\milli\metre} diameter) are attached to equally-spaced disks along the robot backbone at arc-length positions $s \in \{0, 12, 24, 36, 48\}$\,cm, providing discrete samples of the backbone pose. 
An additional rigid body is mounted on the contact object to track its 6-DoF pose within the workspace. The system runs on a dedicated host PC managed by Motive~3.4, which processes and solves 3D pose at \SI{120}{\hertz}. 
Data are streamed in real time to the dataset acquisition PC via a point-to-point Ethernet link 
using the NatNet~4.4 SDK in unicast UDP mode. 
A custom Python client receives each frame and logs the 6-DoF pose --- position $(x, y, z)$ and orientation $(q_x, q_y, q_z, q_w)$ --- of each rigid body, together with a per-body tracking validity flag. 
Timestamps are assigned on the acquisition PC using the system wall clock (\texttt{time.time()}), rather than Motive's internal camera exposure timestamps, which could induce a small and variable 
network transmission jitter in the logged timing.



\subsubsection{Resense FT}

Contact forces between the robot and objects in the workspace were measured using a
Resense HEX\,12 six-axis force/torque sensor (ordering code HEX125S-020S-1R1-1; Resense GmbH, Klingenberg, Germany; \url{https://www.resense.io}).
The HEX\,12 is a compact transducer machined from Titanium Grade~5, instrumented with foil strain gauges, with a diameter of \SI{12.5}{\milli\meter}, a height of \SI{11.0}{\milli\meter},
and a mass of \SI{3.3}{\gram}.
The nominal measurement ranges are $\pm\SI{25}{\newton}$ in $F_x$, $F_y$, and $F_z$, and $\pm\SI{125}{\milli\newton\meter}$ in $T_x$, $T_y$, and $T_z$, with a manufacturer-specified accuracy of $1\%$ of full scale, a crosstalk of $3\%$, and an overload capacity of $300\%$.
The sensor was paired with the Resense miniature evaluation electronics board (EVAL~100S-06-1, $54 \times 25 \times \SI{12.6}{\milli\meter}$), which digitises the strain gauge signals with a 24-bit ADC,
applies the manufacturer-provided calibration matrix onboard, and outputs calibrated forces (in newtons) and torques (in millinewton-metres) as seven 32-bit floating-point values per sample ($F_x$, $F_y$, $F_z$, $T_x$, $T_y$, $T_z$, and temperature).
The electronics was connected to the acquisition computer via its USB interface (\SI{12}{Mbit/s}) appearing as a virtual COM port, and configured at a sample rate of \SI{100}{\hertz}.
A custom Python script reads the 28-byte binary packets from the serial stream
and logs each sample with a wall-clock timestamp.
This sensor was used exclusively in subset~4 (dynamic motions with contact).


\begin{landscape}
\begin{table}[p]
\centering
\caption{Summary of the sensor suite.
Effective sampling rates account for communication overhead and software processing;
native hardware rates may be higher.
Selectable parameters show the value used in this work where known;
the full set of options is given in the corresponding sensor subsection.}
\label{tab:sensor_summary}
\renewcommand{\arraystretch}{1.35}
\footnotesize
\begin{tabular}{@{}
  >{\raggedright\arraybackslash}p{1.8cm}
  >{\raggedright\arraybackslash}p{3.0cm}
  >{\raggedright\arraybackslash}p{1.6cm}
  >{\raggedright\arraybackslash}p{2.8cm}
  >{\raggedright\arraybackslash}p{3.2cm}
  >{\raggedright\arraybackslash}p{2.8cm}
  >{\raggedright\arraybackslash}p{2.5cm}
  >{\raggedright\arraybackslash}p{1.5cm}
  >{\raggedright\arraybackslash}p{1.2cm}
  >{\raggedright\arraybackslash}p{1.2cm}
@{}}
\toprule
\textbf{Sensor} &
\textbf{Model / Manufacturer} &
\textbf{Qty} &
\textbf{Measured quantity} &
\textbf{Range} &
\textbf{Accuracy / Resolution} &
\textbf{Interface} &
\textbf{Eff.\ rate} &
\textbf{Batches} \\
\midrule
%
Cable tension &
Mark-10 M3-5\newline (Mark-10 Corp., USA) &
$\times$\,4 &
Tendon force (1-axis) &
\SIrange{0}{22.2}{\newton}\newline (\SI{5}{lbf} full scale) &
$\pm\,0.3\%$ FS;\newline res.\ \SI{0.02}{\newton};\newline cal.\ unc.\ $\pm$\SI{0.007}{lbf} ($k{=}2$) &
Serial RS-232\newline \SI{115200}{baud} &
${\sim}$\SI{335}{\hertz} &
1--4 \\
\midrule
%
Shape sensing &
FBGS FBG-Scan~904\newline + 4-core MCF fiber\newline (FBGS Int., Belgium) &
1 fiber,\newline 26 DTGs/core &
Distributed curvature, bending angle, 3-D shape &
\SI{50}{\milli\meter} active;\newline \SIrange{1510}{1590}{\nano\meter} &
WL prec.\ \SI{0.3}{\pico\meter} ($1\sigma$);\newline lin.\ \SI{3}{\pico\meter};\newline abs.\ acc.\ \SI{10}{\pico\meter};\newline shape res.\ \SI{1}{\milli\meter} arc &
USB;\newline ILLumiSense Shape CORE v1.1 &
\SI{100}{\hertz} &
1--4 \\
\midrule
%
Base F/T &
ATI Mini40 SI-40-2\newline (ATI Ind.\ Autom., USA)\newline + NI~USB-6210 DAQ &
1 &
6-axis wrench\newline ($F_x,F_y,F_z,T_x,T_y,T_z$)\newline at robot base &
$F_{x,y}$: $\pm$\SI{40}{\newton};\newline $F_z$: $\pm$\SI{120}{\newton};\newline $T_{x,y,z}$: $\pm$\SI{2}{\newton\meter} &
16-bit DAQ (\SI{250}{kS/s});\newline overload $F_{xy}$\,$\pm$\SI{810}{\newton},\newline $F_z$\,$\pm$\SI{2400}{\newton}
% TODO: add resolution from cal. cert.
&
Analog $\pm$\SI{10}{\volt}\newline via \texttt{nidaqmx};\newline HW clk \SI{1000}{\hertz},\newline 5-samp.\ avg. &
${\sim}$\SI{200}{\hertz} &
1--4 \\
\midrule
%
Motion capture &
OptiTrack PrimeX\,13 ($\times$5)\newline + PrimeX\,13W ($\times$2)\newline (NaturalPoint, USA) &
7 cameras;\newline 6 rigid bodies &
6-DoF pose\newline (5 disks + 1 object) &
Sub-metre workspace;\newline 1.3\,MP;\newline \SI{850}{\nano\meter} IR &
$\pm$\SI{0.15}{\milli\meter} (PrimeX\,13);\newline $\pm$\SI{0.30}{\milli\meter} (13W);\newline markers ${\approx}$\SI{6}{\milli\meter} dia. &
GigE/PoE;\newline Motive~3.4;\newline NatNet~4.4 SDK\newline (unicast UDP) &
\SI{120}{\hertz} &
1--4 \\
\midrule
%
IMU &
ISM330DHCX\newline (SparkFun / STMicro) &
$\times$\,4 &
3-axis angular vel.\newline + 3-axis lin.\ accel. &
Gyro: up to $\pm$\SI{4000}{dps};\newline Accel: up to $\pm$\SI{16}{g} &
16-bit ADC
% TODO: confirm selected FS and ODR
&
I\textsuperscript{2}C / SPI &
350\,Hz &
3 \\
\midrule
%
Actuators &
Cybergear QDD\newline (Xiaomi, China) &
$\times$\,4 &
Shaft angle (14-bit),\newline velocity, torque &
Cont.\ \SI{4}{\newton\meter};\newline peak \SI{12}{\newton\meter};\newline spool $r{=}$\SI{20}{\milli\meter} &
14-bit single-turn\newline absolute encoder &
CAN~2.0\newline \SI{1}{Mbit/s} &
460\,Hz &
1--4 \\
\midrule
%
Contact F/T &
Resense HEX\,12\newline (Resense GmbH, Germany)\newline + EVAL~100S-06-1 electronics &
1 &
6-axis wrench\newline ($F_x,F_y,F_z,T_x,T_y,T_z$)\newline at contact point &
$F_{x,y,z}$: $\pm$\SI{25}{\newton}\newline $T_{x,y,z}$: $\pm$\SI{125}{\milli\newton\meter} &
1\% FS;\; 3\% crosstalk;\newline 24-bit ADC;\newline 300\% overload &
USB (\SI{12}{Mbit/s})\newline virtual COM;\newline 28-byte binary packets &
\SI{100}{\hertz} &
4 \\
\bottomrule
\end{tabular}
\end{table}
\end{landscape}
% \begin{table}[p]
% \centering
% \caption{Summary of the sensor suite used in this work.
% Selectable parameters are shown with the full-scale range available
% from the manufacturer datasheet; the values used in this dataset are
% indicated where known.
% Effective sampling rates account for communication overhead and
% software processing; native hardware rates may be higher.}
% \label{tab:sensor_summary}
% \renewcommand{\arraystretch}{1.45}
% \small
% \begin{tabular}{@{} l l @{}}
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Cable tension --- Mark-10 M3-5} (Mark-10 Corporation, Copiague, NY, USA)} \\[2pt]
% \hline
% Quantity measured     & Tendon force (single-axis, $\times$\,4 units) \\
% Full-scale capacity   & \SI{5}{lbf} ($\approx$\,\SI{22.2}{\newton}) \\
% Resolution            & \SI{0.005}{lbf} ($\approx$\,\SI{0.02}{\newton}) \\
% Accuracy              & $\pm\,0.3\%$ of full scale \\
% Calibration uncert.   & $\pm$\,\SI{0.007}{lbf} ($k{=}2$, 95\%, ISO~17025) \\
% Interface             & Serial RS-232, \SI{115200}{baud} \\
% Effective sampling rate & $\sim$\,\SI{335}{\hertz} per gauge \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Shape sensing --- FBGS FBG-Scan 904 + MCF fiber} (FBGS International, Geel, Belgium)} \\[2pt]
% \hline
% Quantity measured     & Distributed curvature, bending angle, 3-D shape \\
% Fiber                 & 4-core MCF, 26 DTGs/core, \SI{2}{\milli\meter} spacing, \SI{50}{\centi\meter} active length \\
% Wavelength range      & \SIrange{1510}{1590}{\nano\meter} (80\,nm bandwidth, 4 channels, LC/APC) \\
% Wavelength precision  & \SI{0.3}{\pico\meter} ($1\sigma$) \\
% Wavelength linearity  & \SI{3}{\pico\meter} \\
% Absolute WL accuracy  & \SI{10}{\pico\meter} \\
% Dynamic range         & ${>}\,$\SI{30}{\decibel} \\
% Shape output resolution & \SI{1}{\milli\meter} arc-length \\
% Software              & ILLumiSense Shape CORE v1.1 \\
% Effective sampling rate & \SI{100}{\hertz} (with shape reconstruction) \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Base force/torque --- ATI Mini40, cal.\ SI-40-2} (ATI Industrial Automation, Apex, NC, USA)} \\[2pt]
% \hline
% Quantity measured     & 6-axis wrench ($F_x, F_y, F_z, T_x, T_y, T_z$) at robot base \\
% Sensing range         & $F_{x,y}$: $\pm$\,\SI{40}{\newton};\; $F_z$: $\pm$\,\SI{120}{\newton};\; $T_{x,y,z}$: $\pm$\,\SI{2}{\newton\meter} \\
% % TODO: fill resolution from calibration certificate
% Resolution            & XXX \\
% Overload (single-axis)& $F_{xy}$: $\pm$\,\SI{810}{\newton};\; $F_z$: $\pm$\,\SI{2400}{\newton} \\
% Resonant frequency    & \SI{3200}{\hertz} ($F_x, F_y, T_z$);\; \SI{4900}{\hertz} ($F_z, T_x, T_y$) \\
% Transducer            & \SI{40}{\milli\meter} dia.\ $\times$ \SI{12.2}{\milli\meter}\,h;\; \SI{49.9}{\gram};\; silicon strain gages \\
% DAQ                   & NI~USB-6210 (16 AI, 16-bit, \SI{250}{kS/s});\; $\pm$\,\SI{10}{\volt} input \\
% Interface             & \texttt{nidaqmx} Python driver;\; HW clock \SI{1000}{\hertz};\; 5-sample averaging \\
% Effective sampling rate & $\sim$\,\SI{200}{\hertz} \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Motion capture --- OptiTrack PrimeX\,13 ($\times$\,5) + PrimeX\,13W ($\times$\,2)} (NaturalPoint, Corvallis, OR, USA)} \\[2pt]
% \hline
% Quantity measured     & 6-DoF rigid-body pose ($\times$\,5 disks $+$ 1 contact object) \\
% Sensor                & 1.3\,MP ($1280 \times 1024$\,px);\; \SI{850}{\nano\meter} IR;\; GigE/PoE \\
% 3-D positional accuracy & $\pm$\,\SI{0.15}{\milli\meter} (PrimeX\,13);\; $\pm$\,\SI{0.30}{\milli\meter} (PrimeX\,13W) \\
% Markers               & Passive retroreflective, $\approx$\,\SI{6}{\milli\meter} dia.;\; 4 per rigid body \\
% Software              & Motive~3.4;\; streamed via NatNet~4.4 SDK (unicast UDP) \\
% Effective sampling rate & \SI{120}{\hertz} \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{IMU --- ISM330DHCX ($\times$\,4)} (SparkFun / STMicroelectronics); batch~3 only} \\[2pt]
% \hline
% Quantity measured     & 3-axis angular velocity $+$ 3-axis linear acceleration \\
% Gyroscope range       & Selectable: $\pm$125 / 250 / 500 / 1000 / 2000 / \SI{4000}{dps} \\
% Accelerometer range   & Selectable: $\pm$2 / 4 / 8 / \SI{16}{g} \\
% ADC resolution        & 16-bit \\
% Interface             & I\textsuperscript{2}C / SPI \\
% % TODO: confirm selected FS, ODR, and placement on disks
% Effective sampling rate & XXX\,Hz \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Actuators --- Cybergear QDD ($\times$\,4)} (Xiaomi, China)} \\[2pt]
% \hline
% Quantity measured     & Shaft angle (14-bit abs.\ encoder), velocity, output torque \\
% Gearbox               & 7.75:1 planetary;\; cont.\ torque \SI{4}{\newton\meter};\; peak \SI{12}{\newton\meter} \\
% Spool winding radius  & \SI{20}{\milli\meter} (custom 3-D printed) \\
% Interface             & CAN~2.0 at \SI{1}{Mbit/s} \\
% % TODO: confirm control cycle rate
% Effective feedback rate & XXX\,Hz \\[4pt]
% \hline\hline
% \\[-8pt]
% \multicolumn{2}{@{}l}{\textbf{Contact force/torque --- Resense FT (XXX)}; batch~4 only} \\[2pt]
% \hline
% Quantity measured     & 6-axis wrench at contact point \\
% % TODO: all Resense specs from Greg
% Sensing range         & XXX \\
% Resolution            & XXX \\
% Interface             & XXX \\
% Effective sampling rate & XXX\,Hz \\[4pt]
% \hline\hline
% \end{tabular}
% \end{table}


% \begin{table}[p]
% \centering
% \caption{Summary of the sensor suite. Selectable parameters are shown
% with the full-scale range available from the datasheet; the values
% used in this work are indicated where known.
% Effective sampling rates account for communication overhead and
% software processing; native hardware rates may be higher.}
% \label{tab:sensor_summary}
% \small
% \begin{tabular}{c c c c c c}
%     %{@{}p{2.1cm} p{2.6cm} p{2.5cm} p{2.2cm} p{1.5cm} p{1.8cm}@{}}
% \hline
% \textbf{Sensor} &
% \textbf{Model} &
% \textbf{Quantity} &
% \textbf{Range} &
% \textbf{Accuracy / Resolution} &
% \textbf{Eff.\ rate (Hz)} \\
% \hline
% %
% Cable tension
%   & Mark-10 M3-5 \newline (Mark-10 Corp.)
%   & Tendon force (1-axis, $\times$\,4)
%   & \SIrange{0}{22.2}{\newton}
%   & $\pm 0.3\%$ FS \newline Res.\ \SI{0.02}{\newton}
%   & $\sim$335 \\[6pt]
% \hline
% Shape sensing
%   & FBGS FBG-Scan~904 + MCF fiber \newline (FBGS Int.)
%   & Distributed curvature, bending angle, 3-D shape
%   & \SI{50}{\centi\meter} active; 26 gratings per core
%   & WL prec.\ \SI{0.3}{\pico\meter}; \newline abs.\ acc.\ \SI{10}{\pico\meter}
%   & 100 \\[6pt]
% \hline
% Base F/T
%   & ATI Mini40 SI-40-2 \newline (ATI Ind.\ Autom.) \newline via NI~USB-6210 DAQ
%   & 6-axis wrench at robot base
%   & $F_{xy}$: $\pm$\SI{40}{\newton} \newline $F_z$: $\pm$\SI{120}{\newton} \newline $T_{xyz}$: $\pm$\SI{2}{\newton\meter}
%   & 16-bit, \SI{250}{kS\per\second} DAQ
% %   TODO: add ATI resolution from calibration certificate
%   & $\sim$200 \\[6pt]
% \hline
% Motion capture
%   & OptiTrack PrimeX~13 ($\times$\,5) + PrimeX~13W ($\times$\,2) \newline (NaturalPoint)
%   & 6-DoF rigid-body pose ($\times$\,5 disks + object)
%   & Sub-metre workspace
%   & $\pm$\SI{0.15}{\milli\meter} (PrimeX~13) \newline $\pm$\SI{0.30}{\milli\meter} (13W)
%   & 120 \\[6pt]
% \hline
% IMU
%   & ISM330DHCX \newline (SparkFun / STMicro), $\times$\,4
%   & 3-axis angular velocity + 3-axis linear acceleration
%   & Gyro: up to $\pm$\SI{4000}{dps} \newline Accel: up to $\pm$\SI{16}{g}
%   & 16-bit ADC
% %   TODO: confirm selected FS and ODR
%   & XXX \\[6pt]
% \hline
% Motor encoders
%   & Cybergear QDD \newline (Xiaomi), $\times$\,4
%   & Shaft angle, velocity, output torque
%   & 14-bit single-turn absolute encoder
%   & ---
%   & XXX \\[6pt]
% \hline
% Contact F/T
%   & Resense FT \newline (XXX)
%   & 6-axis wrench at contact (batch~4 only)
%   & XXX
%   & XXX
%   & XXX \\
% \hline
% \end{tabular}
% \end{table}

\subsection{Experimental setup}
%% NSD GUIDANCE: Describe the physical arrangement AND the set of experiments 
%% performed. This is where a reviewer will assess whether the dataset is 
%% systematic and well-designed.
%%
%%
%% 1. Physical arrangement (prose):
%%    - How the robot is mounted (vertical? horizontal? — the quaternion 
%%      Q_0 = [0.707 0 0.707 0] in the code suggests a 90-deg rotation, 
%%      so the robot hangs or is horizontal — clarify).
%%    - Workspace description: dimensions, what objects are placed for 
%%      contact experiments.
%%    - Cable routing: straight from base to disk, no pulleys — explain why.
%%    - Mocap camera placement and how occlusion was minimized.
%%
%% 2. Table of experiments (STRONGLY RECOMMENDED):
%%    Create a table with columns:
%%      Batch | Trajectory type | Parameters (angle, speed) | Sensors active | 
%%      Number of trials | Duration | With contact? | Notes
%%    Examples from your folder names:
%%      - plane_x_slow, plane_x_angle_150_speed_1, circle_angle_150_speed_3
%%      - Lissajous (mentioned in text)
%%    This table is the single most useful element for a reader deciding 
%%    whether to download and use your dataset.
%%
%% 3. Alignment and calibration protocol (prose):
%%    - How sensors are aligned at the start (first planar motion for 
%%      calibration check).
%%    - How the FBGS reference shape is recorded.
%%    - How the ATI-FT is biased.
%%    - How the OptiTrack rigid-body frames are set to match the robot 
%%      body frame.
%%

% The robot is mounted on a cantilever platform with its tip pointing down (\ie{} in direction of gravity).
% The robot is moved via distributed cable actuation using four tendons (\ie{} two pairs of atagonist tendons). 
% This layout has been chosen as the more representative of the TDCR present in the literature.
% The cables are rolled in the pulley attached to the motor shaft (no gearbox) and routed only throught the pulley used by the Mark 10 sensor.
% This choice was made to reduce the friction of the system as much as possible, removing the inertia of the pulleys and the its friction.
% The workspace of the robot is free but objects are introduced for the contact batch. 
% The object is an extruded aluminium profile provided with two Mocap frames to identify its pose and a force torque sensor to measure the interaction force.
% To provide coverage of the whole workspace, the Mocap camera are placed on the edges on a cube of XxXxX meters. 
% Figure XX represents the camera layout around the TDCR prototype.

% %% 2. Table of experiments:
% %%    Create a table with columns:



% \begin{table}[]
%     \centering
%     \begin{tabular}{|c|c|c|c|c|c|c|}
%         Batch & Trajectory type & Parameters & Sensors active & Number of trials & Duration & Contact \\
%          & & & & & &  
%     \end{tabular}
%     \caption{Caption}
%     \label{tab:placeholder}
% \end{table}


% %%
% %% 3. Alignment and calibration protocol (prose):
% %%    - How sensors are aligned at the start (first planar motion for 
% %%      calibration check).
% %%    - How the FBGS reference shape is recorded.
% %%    - How the ATI-FT is biased.
% %%    - How the OptiTrack rigid-body frames are set to match the robot 
% %%      body frame.
% %%
% In order to ensure alignment of the sensor data, every recording requires the following steps.
% First, the cables are pre-tensioned with a delta between the two tendon tension $< 0.2$ N, with an initial tension for each cable of approx $1 N$, this minimises the possibility of slack, a well known problem for TDCR [reference].
% Secondly, the robot starting straight down allows to handle any misalignment with the kinematics measures of the mocap frames and FBGS reconstructed shape.
% Finally, for every non-planar motion (\ie{} circular and Lijassous curves) the robot starts a calibration routine moving first in the $x-z$ plane (starting moving in positive $x$ direction following by negative $x$ direction) and then on the $y-z$ plane (starting moving in negative $y$ direction following by positive $y$ direction).
% This first routine allows to overcome any misalignment issue that could affect the FBGS sensor (as aligning a fibre to the robot is challenging and almost always leads to some residual error). This allows to determine the deformation planes of the fiber before starting the actual robot motion.

% The force-torque sensors are biased and a provided calibration matrix is used to overcome this issue. For the ATI FT ... for the Resense FT ... expand this paragraph.

% Finally, the mocap frames are defined by adding some reflecting markers to the robot disks with appropriate 3D-printed extensions. 
% Five disks are provided with four markers each to uniquely identify a frame that is attached to the center of the merkers (\ie{} the backbone).

% The gyroscopes are placed at a known location on the robot disk, allowing to transport the computed accelerations to the robot backbone (the center of the mocap frame).


% % \begin{itemize}
% %     \item Details on the designed base (cables without pulleys, sturdy, non-occlusion for mocap)
% %     \item Experiments performed (static loads, planar motions, circle, Lissajous curve) with and without gyroscopes and with interaction with object in workspace.
% %     \item Experiments starting with planar motion first (alignment of sensors)
% % \end{itemize}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\linewidth]{figures/fig_robotPlatform.pdf}
    \caption{Hardware platform: TDCR with four tendons (two antagonistic pairs), motion capture markers on selected disks, direct-drive motor spools, and inline Mark-10 tension sensing and ATI F/T placed at the base.}
    \label{fig:robotPlatform}
\end{figure}

The robot is mounted on a cantilever platform with its tip pointing downward, \ie{} in the direction of gravity (see Fig. \ref{fig:robotPlatform}). %A dedicated rigid aluminium actuation structure hosts the motors and spooling units to ensure repeatable measurements.
It is actuated via four tendons arranged in two antagonistic pairs.
This layout was chosen as representative of the tendon-driven continuum robots commonly studied in the literature.
Each tendon is wound on a spool attached directly to the motor shaft (no gearbox) and routed only through the pulley integrated in the Mark~10 force gauge. Tendon length variations are related to commanded motor rotations through a fixed spool radius ($r_{\mathrm{spool}} = 20$~mm), providing a direct mapping between motor shaft angle and tendon displacement.
No additional pulleys or redirections are used, so as to minimise cable friction and eliminate the reflected inertia of intermediate transmission elements. Every other spacer disk is embedded with retroreflective markers and tracked by an OptiTrack motion capture system, providing 6-DOF pose measurements for the tracked disks.
%
In batches~1--3 the workspace is free of obstacles.
In batch~4, an extruded aluminium profile is introduced as a contact object.
The profile is equipped with two motion capture frames to identify its pose and with a Resense force-torque sensor to measure the interaction force.
%TODO: add object dimensions

%%  --- TABLE OF EXPERIMENTS ---

\begin{table}[ht]
\scriptsize
\centering
\caption{Sensors active per trajectory and batch.
Each dynamic trajectory (batches 2--4) is executed at two actuator speed limits:
slow ($\omega_{\max} = 1$~rad/s) and fast ($\omega_{\max} = 3$~rad/s).
Recording duration is 30~s per trial; motor amplitude is $\pi$~rad for all trajectories.}
\label{tab:experiments}
\renewcommand{\arraystretch}{1.3}
\footnotesize
\begin{tabular}{@{} l p{3.8cm} p{2.1cm} p{2.1cm} p{2.2cm} p{2.2cm} @{}}
\toprule
\textbf{Trajectory} & \textbf{Description}
  & \textbf{B1} \newline \textbf{Quasi-static}
  & \textbf{B2} \newline \textbf{Dynamic}
  & \textbf{B3} \newline \textbf{Dyn.\,+\,IMU}
  & \textbf{B4} \newline \textbf{Dyn.\,+\,contact} \\
\midrule
\texttt{plane\_x}  & Planar oscillation in the $x$--$z$ plane
  & M, F, A, O, E & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\texttt{plane\_y}  & Planar oscillation in the $y$--$z$ plane
  & M, F, A, O, E & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\texttt{plane\_xy} & Diagonal planar oscillation ($x$+$y$)
  & M, F, A, O, E & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\texttt{circle}    & Circular path (8 waypoints, $45^\circ$ steps)
  & N/A & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\texttt{Lissajous} & 2:1 Lissajous figure-eight (16 waypoints)
  & N/A & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\texttt{star}      & Star/cross: $x$, $y$, and both diagonals
  & N/A & M, F, A, O, E & M, F, A, O, E, I & M, F, A, O, E, R \\
\bottomrule
\end{tabular}
\par\vspace{4pt}
{\scriptsize Sensor key: M\,=\,Mark10 cable tension ($\times$4), F\,=\,FBGS shape,
A\,=\,ATI-FT base wrench, O\,=\,OptiTrack MoCap, E\,=\,motor encoders,
I\,=\,IMU ($\times$4, batch~3 only), R\,=\,Resense contact F/T (batch~4 only).}
\end{table}

%%  --- ALIGNMENT AND CALIBRATION PROTOCOL ---
To ensure consistency across all sensor modalities, every recording session follows a fixed preparation protocol.

First, the four tendons are pre-tensioned to approximately \SI{1}{\newton} each, with a maximum difference between the two cables of an antagonistic pair $<$\SI{0.2}{\newton}.
This step minimises the risk of cable slack, a well-known source of modelling error in tendon-driven continuum robots~\cite{Rucker2011a}.

Second, the recording begins with the robot hanging straight down under gravity.
This known reference configuration is used to verify the alignment between the OptiTrack rigid-body frames and the FBGS-reconstructed shape: any offset observed in the straight configuration is attributable to sensor misalignment rather than to robot deformation.

Third, for every non-planar trajectory (\ie{} circular and Lissajous curves) the robot executes a calibration routine before the target motion.
The routine consists of a planar sweep in the $x$--$z$ plane (positive $x$ followed by negative $x$), then a planar sweep in the $y$--$z$ plane (negative $y$ followed by positive $y$).
Because aligning the FBGS fibre to the robot backbone with sub-degree accuracy is difficult in practice, a residual angular offset between the fibre deformation plane and the robot bending plane is expected.
The calibration sweeps characterize this offset, allowing it to be corrected in post-processing before the non-planar data is interpreted.

% The force-torque sensors are prepared as follows.
% For the ATI-FT at the robot base, a bias measurement is recorded with the robot at rest before each experiment; the bias is subtracted from all subsequent readings, and the manufacturer-provided calibration matrix is applied to convert corrected voltages to forces and torques.
% For the Resense FT at the contact object~{\color{red}[expand: same procedure? different?]}.

% Finally, the OptiTrack rigid-body frames are defined by attaching four reflective markers to five disks via 3D-printed extensions.
% The four markers per disk uniquely define a reference frame whose origin is registered to the geometric centre of the disk, \ie{} the backbone centreline.
% The IMU sensors are mounted at a known offset from the disk centre, so that the measured accelerations can be transformed to the backbone centreline using a rigid-body transfer.


\subsection{Data Collection}
%% NSD GUIDANCE: "full descriptions of [...] data acquisition and any 
%% computational processing."
%%
%%
%% 1. Acquisition pipeline:
%%    - Software: Python scripts for motor control and sensor reading 
%%      (reference the code repository), MATLAB for post-processing.
%%    - OS and hardware: what computer, OS version.
%%    - How each sensor is read: serial, TCP, SDK, etc.
%%    - Sampling rates: each sensor has its own native rate (report in a 
%%      table or refer to the Sensors subsection table).
%%
%% 2. Synchronization:
%%    - All sensors are timestamped with the CPU wall-clock at read time.
%%    - State the expected synchronization accuracy (bounded by the 
%%      acquisition loop period and OS scheduling jitter — give an 
%%      estimate, e.g., <5 ms).
%%    - State that no hardware trigger is used.
%%
%% 3. Post-processing (describe what process_data.m does):
%%    - Butterworth low-pass filter: order 4, cutoff 20 Hz, zero-phase 
%%      (filtfilt).
%%    - Resampling: all signals interpolated to a uniform 100 Hz grid.
%%    - Coordinate transformations: OptiTrack frames converted to body-
%%      relative poses using the first N_frames_static_begin frames.
%%    - FBGS shape rotation (the TODO in your code about rotating the 
%%      FBGS x-axis down).
%%    - State that BOTH raw and processed data are released.
%%
%% 4. File naming convention:
%%    - Describe the naming pattern (e.g., dataMotor.csv, dataMark10_+x.csv, 
%%      dataATIFT.csv, dataOptiTrack.csv, dataFBGS.csv).
%%    - Describe the processed/ subfolder contents.
%%




\begin{figure}[t]
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip plane_x_slow.png}
        \caption{}
        \label{fig:motion plane x}
    \end{subfigure}%
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip plane_y_slow.png}
        \caption{}
        \label{fig:motion plane y}
    \end{subfigure}%
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip plane_xy_slow.png}
        \caption{}
        \label{fig:motion plane xy}
    \end{subfigure}
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip star_slow.png}
        \caption{}
        \label{fig:motion star}
    \end{subfigure}%
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip circle_slow.png}
        \caption{}
        \label{fig:motion circle}
    \end{subfigure}%
    \begin{subfigure}[t]{.33\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/Tip Lissajous_slow.png}
        \caption{}
        \label{fig:motion Lissajous}
    \end{subfigure}
    \caption{Motion of the robot tip (only $xy$ plane coordinates) for the planar motions on $x$ (\subref{fig:motion plane x}), $y$ (\subref{fig:motion plane y}) and $xy$ (\subref{fig:motion plane xy}), the star motion (\subref{fig:motion star}), the circular (\subref{fig:motion circle}) and Lissajous (\subref{fig:motion Lissajous}) trajectories.}
\end{figure}


\subsection{Initial Calibration}
In order to ensure the validity of the results, before running any subset, the robot undergoes manual calibration.
First, the robot base is leveled using "una bolla".
Secondly, the robot is ensured to be in a straight position using a laser level positioned on a leveled ground. 
For this aim, we used a Bosh XX.YY level laser.
In order to conpensate for some residual bending on the robot, one cable has been pulled until straight configuration is reached.
In this configuration, the mocap frames have been ensured to be aligned while, in the ShapeCore software, the "set straight" option was selected.

With these adjustments, the robot is ensured to be straight and algined with the sensors measurements.
In the data secording, the cables are pretensioned to avoid slacking. 
With the so proposed straight calibration any consequent bending on the robot will be registered by the sensors and will be adequately recorded. 

%%  --- ACQUISITION PIPELINE ---
\subsubsection{Acquisition pipeline}
For each experiment, a Windows batch script orchestrates the acquisition.
The script launches one independent process per sensor, each in its own terminal window.
Each sensor process polls its instrument at the native sampling rate (see the sensor specifications in Table \ref{tab:sensor_summary}), buffers all readings in memory, and writes them to a CSV file after the recording is complete.
The motor process is launched last, and waits \SI{2}{\second} before sending position commands, providing a sufficient delay to ensure all sensors to be already streaming before the actuators begin to move.
%% TODO: confirm the startup delay value. The original draft said "2 seconds";
%% the code shows time.sleep(1) for homing at the end, but the main control 
%% loop appears to start immediately after CAN bus init. Clarify whether there 
%% is an explicit sleep before the first motor command or just the natural 
%% startup latency.

Two sensors require dedicated proprietary software that runs independently from the
per-sensor Python acquisition scripts.
The FBGS fibre is managed by the ILLumiSense Shape~CORE software (v1.1, FBGS),
which runs as a background process on the acquisition computer, connects to the
FBG-Scan~904 interrogator via USB, performs peak detection on the acquired spectra,
and reconstructs curvature, bending angle, and three-dimensional shape in real time.
Shape~CORE streams its output over a local TCP connection (port~50012).
A custom C++ client (\texttt{StreamClassTester}) connects to this stream,
receives the curvature, angle, and shape arrays at each time step,
timestamps each sample using the system high-resolution clock
(\texttt{std::chrono::high\_resolution\_clock}), and buffers all data in memory
until the recording is complete, at which point it writes a single CSV file.
%
The OptiTrack motion capture system is driven by Motive~3.4 (NaturalPoint, USA)
running on a dedicated Windows host PC.
Motive processes and solves the 3-D rigid-body poses at \SI{120}{\hertz} and
exposes them through the NatNet~4.4 SDK in unicast UDP mode.
A dedicated Python script on the acquisition machine receives each frame over a
point-to-point Ethernet link and timestamps it with the wall-clock of the
acquisition machine (\texttt{time.time()}), ensuring a homogeneous time base
across all sensor modalities.

%%  --- SYNCHRONIZATION ---
\subsubsection{Synchronization}
All timestamps in the recorded CSV files are therefore referred to the same operating-system clock, without the need for a hardware trigger.
The synchronization accuracy is bounded by the OS thread-scheduling jitter, on the order of \SI{1}{\milli\second} to \SI{2}{\milli\second} on the acquisition machine.


%%  --- POST-PROCESSING ---
\subsubsection{Post processing}
The raw sensor data is post-processed in MATLAB (\texttt{process\_data.m}).

% For the motion capture data, frames in which one or more markers are occluded are first discarded using the validity flag provided by the tracking software (a value of zero indicates occlusion).
The remaining samples are converted from position ($x$, $y$, $z$) and quaternion to $\mathrm{SE}(3)$ poses.
Because these poses are expressed in the global motion-capture frame, they are re-expressed as transformations relative to the base disk (disk~0).
%A static rigid-body transformation is then applied to orient the $z$-axis downward (the gravity direction) and to align the $x$- and $y$-axes with the bending-plane convention of the robot.

The FBGS data requires an additional alignment step.
The interrogator software reconstructs the fibre shape with the arc-length parameter evolving along its internal $x$-axis, whereas the physical robot hangs along negative~$z$; a rigid rotation is therefore applied to map the FBGS coordinate frame to the robot body frame.
Furthermore, because the fibre cannot be bonded to the backbone with sub-degree angular accuracy, a residual rotational offset exists between the fibre deformation plane and the true robot bending plane.
To characterise this offset, a singular-value decomposition is performed on the tip trajectory during the initial planar calibration sweep of each batch: the principal directions of the tip motion identify the bending-plane normal and the primary deflection axis, yielding the correction rotation that aligns the FBGS output to the robot frame.

All signals are then low-pass filtered with a fourth-order Butterworth filter (cutoff frequency \SI{20}{\hertz} \textbf{Angela: add rationale cutoff frequency with spctral analysis}) applied in the forward and reverse directions (\texttt{filtfilt} in MATLAB) to achieve zero-phase distortion.
The filter's normalised cutoff is computed per signal using each sensor's native sampling rate, estimated from the median inter-sample interval.
The filtered signals are aligned in time by expressing all timestamps relative to the first actuator timestamp; because every sensor starts recording before the motors, these relative timestamps are initially negative.
All signals are then resampled onto a uniform \SI{100}{\hertz} grid spanning $t = 0$ to the end of the actuator recording using linear interpolation (\texttt{interp1}).
This operation simultaneously discards the sensor preamble (the negative-time samples recorded before the motors started) and brings all modalities to a common time base.

%%  --- FILE NAMING AND OUTPUTS ---
\subsubsection{File naming and outputs}
Each sensor process writes one CSV file per experiment, with the following naming convention \texttt{data\{Sensor\}.csv}.
For example, \texttt{dataMotor.csv}, \texttt{dataMark10\_+x.csv}, \texttt{dataATIFT.csv}, \texttt{dataOptiTrack.csv}, and \texttt{dataFBGS.csv}.
Column headers and units are documented in the Data Records section.
The post-processed data is saved in a \texttt{processed/} subfolder within each experiment directory, containing one file per quantity (\texttt{angles.csv}, \texttt{cable\_tensions.csv}, \texttt{base\_wrench.csv}, \texttt{optitrack\_frames.csv}, \texttt{fbgs\_shapes.csv}), all sampled at \SI{100}{\hertz} with a shared time column.
Both the raw and processed datasets are released.


\section{Data Records}

The dataset is hosted on figshare at \url{https://doi.org/10.6084/m9.figshare.XXXXXXX} (Ref.~[XX]); acquisition and post-processing code is available on GitHub (see Code Availability).
%% TODO: replace the DOI placeholder once the figshare deposit is finalized.
The repository is organised into four top-level batches: \texttt{batch\_1} (quasi-static, no contact), \texttt{batch\_2} (dynamic, no contact), \texttt{batch\_3} (dynamic, body-mounted IMUs), and \texttt{batch\_4} (dynamic, with external contact).
Within each batch, sub-folders are named \texttt{<trajectory>\_<speed>}, where \texttt{<trajectory>} $\in \{$\texttt{plane\_x}, \texttt{plane\_y}, \texttt{plane\_xy}, \texttt{circle}, \texttt{Lissajous}, \texttt{star}$\}$ and \texttt{<speed>} $\in \{$\texttt{slow} ($\omega_{\max} = 1$\,rad/s), \texttt{fast} ($\omega_{\max} = 3$\,rad/s)$\}$.
Every experiment folder contains the raw sensor CSV files, a \texttt{processed/} sub-folder with data filtered and uniformly resampled at \SI{100}{\hertz} (time origin $t = 0$ at the first motor command), and a \texttt{gvs/} sub-folder with GVS forward-dynamics simulation outputs.
All raw CSV files carry a header row with explicit column names and units; timestamps are POSIX wall-clock seconds drawn from a single OS clock shared across all sensor processes.
Table~\ref{tab:data_records} documents every file type, its columns, units, and content.

\begin{longtable}{@{} p{3.8cm} p{4.8cm} p{1.2cm} p{5.6cm} @{}}
\caption{Complete file and column documentation for each experiment folder.
Subscript $n \in \{1,2,3,4\}$ denotes motor index (motors 1\,\&\,3 form the $x$-plane pair; 2\,\&\,4 the $y$-plane pair).
Subscript $k \in \{0,\ldots,4\}$ denotes disk index from base to tip.
Raw files include a header row; processed files are headerless.}
\label{tab:data_records}\\
\toprule
\textbf{File} & \textbf{Column(s)} & \textbf{Unit} & \textbf{Description} \\
\midrule
\endfirsthead
\multicolumn{4}{c}{\small\tablename~\thetable\ \textit{(continued)}}\\[2pt]
\toprule
\textbf{File} & \textbf{Column(s)} & \textbf{Unit} & \textbf{Description} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r@{}}{\small\textit{Continued on next page}}\\
\endfoot
\bottomrule
\endlastfoot
%% ── Raw files ──────────────────────────────────────────────────────────────
\multicolumn{4}{@{}l}{\textbf{Raw sensor files}} \\[2pt]
\texttt{dataMotor.csv}
  & \texttt{timestamp}                       & s     & CPU wall-clock (POSIX) \\
  & \texttt{phase\_index}                    & --    & Trajectory phase (0\,=\,idle; successive integers = motion segments) \\
  & \texttt{home$n$} \quad ($n=1$--$4$)      & bool  & Homing completed flag, motor $n$ \\
  & \texttt{target$n$\_rad}                  & rad   & Commanded shaft angle, motor $n$ \\
  & \texttt{abs\_angle$n$\_rad}              & rad   & Absolute encoder reading, motor $n$ \\
  & \texttt{rel\_angle$n$\_rad}              & rad   & Relative angle from home, motor $n$ \\
  & \texttt{velocity$n$\_rad\_s}             & rad/s & Shaft angular velocity, motor $n$ \\
  & \texttt{torque$n$\_Nm}                   & N\,m  & Output torque, motor $n$ \\[4pt]
\texttt{dataMark10\_$\ast$.csv} \newline (4 files: \texttt{+x}, \texttt{-x}, \texttt{+y}, \texttt{-y})
  & \texttt{timestamp}                       & s     & CPU wall-clock (POSIX) \\
  & \texttt{tension (N)}                     & N     & Tendon tension; file suffix indicates tendon direction \\[4pt]
\texttt{dataATIFT.csv}
  & \texttt{timestamp}                       & s     & CPU wall-clock (POSIX) \\
  & \texttt{Fx (N)}, \texttt{Fy (N)}, \texttt{Fz (N)}       & N     & Base reaction force along $x$, $y$, $z$ \\
  & \texttt{Tx (Nm)}, \texttt{Ty (Nm)}, \texttt{Tz (Nm)}    & N\,m  & Base reaction torque about $x$, $y$, $z$ \\[4pt]
\texttt{dataOptiTrack.csv}
  & \texttt{timestamp\_s}                    & s     & CPU wall-clock (POSIX) \\
  & \texttt{disk\_$k$\_frame\_number}        & --    & Motive software frame counter \\
  & \texttt{disk\_$k$\_is\_valid}            & bool  & Tracking validity (0\,=\,occluded) \\
  & \texttt{disk\_$k$\_x/y/z}               & m     & 3-D position of disk $k$ \\
  & \texttt{disk\_$k$\_q[xyzw]}             & --    & Orientation quaternion, scalar-last convention \\[4pt]
\texttt{dataFBGS.csv} \newline (1\,559 columns total)
  & \texttt{Timestamp}                       & s     & CPU wall-clock (POSIX) \\
  & \texttt{Curvature\_0}\,--\,\texttt{\_25} (26) & 1/m & Curvature at each of the 26 fibre gratings \\
  & \texttt{Angle\_0}\,--\,\texttt{\_25} (26)    & rad  & Bending angle at each grating \\
  & \texttt{Shape\_0}\,--\,\texttt{\_1505} (1\,506) & m & Flattened $[x,y,z]$ triplets of the reconstructed 3-D backbone shape at \SI{1}{\milli\meter} arc-length resolution (ILLumiSense Shape\,CORE output) \\[6pt]
%% ── Processed files ────────────────────────────────────────────────────────
\multicolumn{4}{@{}l}{\textbf{Processed files (\texttt{processed/}) — headerless CSV, 100\,Hz, $t=0$ at first motor command}} \\[2pt]
\texttt{angles.csv}
  & col\,1: \texttt{time}                    & s     & Time \\
  & cols\,2--5: $\theta_1, \theta_2, \theta_3, \theta_4$ & rad & Motor shaft angles relative to home \\[4pt]
\texttt{cable\_tensions.csv}
  & col\,1: \texttt{time}                    & s     & Time \\
  & cols\,2--5: $T_{+x}, T_{+y}, T_{-x}, T_{-y}$ & N & Low-pass filtered tendon tensions \\[4pt]
\texttt{base\_wrench.csv} \newline \texttt{base\_wrench\_raw.csv}
  & col\,1: \texttt{time}                    & s     & Time \\
  & cols\,2--4: $F_x, F_y, F_z$              & N     & Base reaction force \\
  & cols\,5--7: $T_x, T_y, T_z$             & N\,m  & Base reaction torque; \texttt{\_raw} variant is bias-subtracted only (no low-pass filter) \\[4pt]
\texttt{mocap\_frames.csv}
  & col\,1: \texttt{time}                    & s     & Time \\
  & $x_k, y_k, z_k$ \,($k=0$--$4$)          & m     & Disk position in robot base frame \\
  & $q_{x,k},q_{y,k},q_{z,k},q_{w,k}$       & --    & Disk orientation quaternion \\
  & $s_k$                                    & m     & Arc-length coordinate of disk $k$ \\[4pt]
\texttt{fbgs\_shapes.csv}
  & col\,1: \texttt{time}                    & s     & Time \\
  & cols\,2--175: $[x_i,y_i,z_i]$, $i=0$--$57$ & m & Reconstructed 3-D backbone at \SI{1}{\milli\meter} resolution, aligned to robot base frame \\[4pt]
\texttt{RMSEs.txt}
  & \multicolumn{3}{p{11.6cm}@{}}{Plain-text report: tip-position RMSE (OptiTrack vs.\ FBGS, in m and \% of motion range) and cable-displacement RMSE (encoder vs.\ motion-capture reconstruction, in m and \%)} \\[4pt]
%% ── GVS outputs ────────────────────────────────────────────────────────────
\multicolumn{4}{@{}l}{\textbf{Simulation outputs (\texttt{gvs/})}} \\[2pt]
\texttt{simulation\_results.mat}
  & \multicolumn{3}{p{11.6cm}@{}}{MATLAB workspace containing GVS forward-dynamics outputs (predicted tip trajectory and base wrench) used for the force--kinematics consistency check (Section~\ref{sec:force_kin_consistency})} \\
\end{longtable}

% \section{Data Overview (optional)}
\section{Technical Validation}
%
% NSD GUIDANCE: This section should describe experiments, analyses, or checks 
% needed to support the technical quality of the dataset. It is NOT a results 
% section: present only what is necessary to convince the reviewer that the 
% recorded data is internally consistent and trustworthy.
%
% STRUCTURE RATIONALE (for co-authors):
%   - 5.1: Sensor-level checks (noise, calibration) — each sensor in isolation
%   - 5.2: Kinematic cross-validation (OptiTrack vs FBGS vs IMU) — sensors agree
%   - 5.3: Force–kinematics consistency via GVS — the ONLY way to cross-check 
%          force and shape data, framed as a data consistency tool
%   - 5.4: Contact force static check — validates the Resense FT readings
%   - 5.5: Synchronization — timestamps are aligned across all sensors
%   - 5.6: Repeatability — same trajectory gives similar recordings
%


\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{figures/Lissajous_slow/Tip Position.png}
    \caption{Trajectory of the robot tip as measured by the OptiTrack motion capture system (blue) the FBGS (green) and simulated with the GVS approach fed with tendon tensions (red) using the Lijassous motion.}
    \label{fig:tip trajectory Lissajous_slow}
\end{figure}

% \begin{figure}
%     \centering
%     \includegraphics[width=0.8\linewidth]{figures/MoCap vs Motor tendon tension.png}
%     \caption{Comparison of the pulled lenght measured from the actuators and reconstructed from the OptiTrack system.}
%     \label{fig:cable lenght}
% \end{figure}

% \begin{figure}
%     \begin{subfigure}[t]{.5\linewidth}
%         \includegraphics[width=\linewidth]{figures/Lissajous_slow/Torque.png}
%         \caption{}
%         \label{fig:Base Torque}
%     \end{subfigure}%
%     \begin{subfigure}[t]{.5\linewidth}
%         \includegraphics[width=\linewidth]{figures/Lissajous_slow/Cable Displacement.png}
%         \caption{}
%         \label{fig:cable displacement}
%     \end{subfigure}%
%     \caption{Comparison of the simulated trque at the base (\subref{fig:Base Torque}) and cable displacement (pulled length) in (\subref{fig:cable displacement}).}
%     \label{fig:cable and torque}
% \end{figure}
\begin{figure}
    \centering
    \includegraphics[width=.8\linewidth]{figures/Lissajous_slow/Torque.png}
    \caption{Comparison of the simulated torque at the base (red) with the measured torque by the (unfiltered) ATI FT sensor (green) using the Lissajous motion.}
    \label{fig:Base Torque}
\end{figure}


This section presents the analyses performed to verify the technical quality and internal consistency of the recorded dataset.
The dataset contains two families of measurements: kinematic quantities (OptiTrack poses, FBGS strain and reconstructed shape, IMU accelerations, motor encoder angles) and force quantities (Mark10 cable tensions, ATI-FT base wrench, Resense contact force).
Within each family, sensors may be directly compared.
However, no direct comparison is possible between kinematic and force measurements without invoking the governing physics of the continuum robot.
For this reason, a reduced-order dynamics model is employed as a cross-validation tool to verify force--kinematics consistency, as detailed in Section~\ref{sec:force_kin_consistency}.

\subsection{Sensor noise and calibration}\label{sec:sensors_noise}
%% TODO: For each sensor, report a short static recording and the resulting 
%% noise floor (standard deviation). If any sensor-specific calibration was 
%% performed (e.g., ATI-FT bias removal, FBGS zero-strain reference, Mark10 
%% zeroing), describe it here.
%% 
%% Suggested content:
%%   - Table with columns: Sensor | Quantity | Sampling rate | Static noise (std)
%%   - Mention any offset/bias subtraction applied to raw data (e.g., the ATI 
%%     bias recorded before each experiment)
%%   - Note the Mark10 zeroing procedure and its accuracy
%%   - FBGS reference shape acquisition and its repeatability

For each experiment, the robot remains static for approximately \SI{3}{\second} before the motors begin to move.
This preamble provides a per-experiment estimate of the static noise floor for every sensor.
In addition, dedicated static recordings (robot at rest, all sensors active) were collected to characterise sensor noise and calibration offsets independently of the dynamic experiments.
Table~\ref{tab:sensor_noise} summarises the static noise levels measured during a representative \SI{3}{\second} window.

\begin{table}[ht]
\centering
\caption{Static noise characterisation.
Values are standard deviations computed over a \SI{3}{\second} window with the robot at rest.
Force and torque values are reported after bias subtraction.}
\label{tab:sensor_noise}
\renewcommand{\arraystretch}{1.2}
\footnotesize
\begin{tabular}{@{} l l l @{}}
\toprule
\textbf{Sensor} & \textbf{Quantity} & \textbf{Static noise ($1\sigma$)} \\
\midrule
Mark10 ($\times$4) & Cable tension & $< \SI{0.05}{\newton}$ \\
ATI-FT           & $F_x, F_y$   & $\sim\SI{0.02}{\newton}$ \\
                 & $F_z$        & $\sim\SI{0.03}{\newton}$ \\
                 & $T_x, T_y, T_z$ & $\sim\SI{0.001}{\newton\meter}$ \\
OptiTrack        & Position ($x,y,z$) & $< \SI{0.15}{\milli\meter}$ \\
                 & Orientation ($q$)  & $< 0.1^\circ$ \\
FBGS             & Tip position ($x,y,z$) & $\sim\SI{0.3}{\milli\meter}$ \\
Motor encoder    & Shaft angle   & $< \SI{0.001}{\radian}$ \\
%% TODO: fill in measured values from the static recordings once available.
%% The values above are order-of-magnitude estimates based on the sensor datasheets.
\bottomrule
\end{tabular}
\end{table}

The OptiTrack rigid-body poses exhibit a slow positional drift on the order of $\sim\SI{0.1}{\milli\meter}$ over several minutes, attributable to thermal expansion in the camera mounts and minor calibration drift.
This drift is negligible relative to the robot displacements during dynamic motions but may be observable in long static recordings.
The Mark10 gauges were zeroed before each experiment; residual offsets after zeroing are below the sensor resolution (\SI{0.02}{\newton}).
The ATI-FT bias was recorded at rest before every acquisition session and subtracted in software.

\subsection{Kinematic cross-validation}
\label{sec:kin_cross_val}
%% Compare shape/pose measurements from independent kinematic sensors.
%%
%% 1) OptiTrack vs FBGS:
%%    - Compare the tip position (and intermediate disk positions if available) 
%%      recorded by OptiTrack with the FBGS-reconstructed shape evaluated at the 
%%      same arc-length coordinates.
%%    - Quantify the discrepancy (e.g., RMSE of tip position over time).
%%    - Include a figure: overlay of OptiTrack and FBGS tip trajectories vs time 
%%      (the code in process_data.m lines 43–68 already produces this).
%%
%% 2) Numerical derivatives of OptiTrack vs IMU:
%%    - Compute velocity and acceleration from OptiTrack position via numerical 
%%      differentiation (central differences on the filtered signal).
%%    - Compare the resulting acceleration with the IMU measurements at the 
%%      corresponding disk.
%%    - Report the agreement qualitatively (overlay plot) and quantitatively (RMSE).
%%
%% 3) Motor encoder vs cable displacement:
%%    - Convert motor angles to cable displacements using the spool radius.
%%    - Compare with the cable length changes inferred from the robot shape 
%%      (if computable from OptiTrack/FBGS data).

\subsubsection{Comparison of recorded shape with FBGS and Motion Capture}

The OptiTrack and FBGS sensors provide independent measurements of the robot shape.
To verify their mutual consistency, we compare the tip position recorded by OptiTrack with the tip position reconstructed from FBGS strain data, evaluated at the same arc-length coordinate.
Figure~\ref{fig:tip trajectory Lissajous_slow} shows the overlay of these two signals for the Lissajous trajectory (see Fig. \ref{fig:motion Lissajous}).
The overlay show consistency between the two measurements, where deviations could be attributed to the disks drift observed by the static data discussed in Section \ref{sec:sensors_noise}.
When evaluating the $(x,\;y,\;z)$ position of the robot tip, the RMSE is $(0.0172,\;0.0140,\;0.0085)$ m.


\subsubsection{Validation of Cable Displacement via Motion Capture}

\begin{figure}
    \begin{subfigure}[t]{.5\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/plane_x/cable_x.png}
        \caption{}
        \label{fig:cable x}
    \end{subfigure}%
    \begin{subfigure}[t]{.5\linewidth}
        \includegraphics[width=\linewidth]{figures/robot_motion/plane_y/cable_y.png}
        \caption{}
        \label{fig:cable y}
    \end{subfigure}%
    \label{fig:cable lenght}
    \caption{Comparison of the pulled cable lenght as per measured from the actuator encoders (red) and reconstructed from the OptiTrack recorded kinematics (blue) for the \textit{plane $x$} (\subref{fig:cable x}) and \textit{plane $y$} (\subref{fig:cable y}).}
\end{figure}


An independent geometric estimate of the cable displacement is derived from the motion-capture data to cross-validate the encoder-based measurements recorded by the actuators.
The continuum robot is equipped with four antagonistic cables routed
parallel to the elastic backbone and offset by $d = \SI{37.5}{mm}$ from its centreline along the $\pm x$ and $\pm y$ directions in the local cross-sectional frame.
Five rigid spacer disks, instrumented with reflective markers, are tracked by the optical motion-capture system at $\SI{100}{Hz}$ and their six-degree-of-freedom poses are reconstructed relative to the robot base frame.
%Intrinsic $XYZ$ Euler angles are extracted from each disk's rotation matrix and phase-unwrapped over time to remove artificial discontinuities at the $\pm\pi$ boundary; the yaw component (rotation about the backbone axis) is subsequently forced to zero, consistent with the assumption that the robot undergoes purely spatial bending without torsion.

To obtain a dense description of the backbone geometry, the five measured disk frames are interpolated along a normalised arc-length parameter $s \in [0,\,1]$ onto \num{40} uniformly spaced points.
Positions are interpolated with a shape-preserving piecewise-cubic Hermite (PCHIP) scheme, while orientations are interpolated by converting each rotation matrix to a unit quaternion, applying PCHIP independently to each quaternion component, and renormalising the result to the unit sphere.
Quaternion hemisphere consistency is enforced between successive disks to avoid sign-flip artefacts.
At each interpolated frame the world-space position of every cable is computed by applying the local offset vector $\mathbf{d}_c$ through the interpolated rotation matrix.
The total length of cable $c$ is then approximated as the piecewise-linear sum of Euclidean distances between consecutive attachment points along the interpolated backbone.

The cable displacement is defined as the change in length relative to the straight (pre-motion) configuration, estimated by averaging the first $N_{\mathrm{ref}} = 10$ frames:
\begin{equation}
    \Delta L_c(t) \;=\; L_c(t) - \bar{L}_c^{\,\mathrm{ref}},
    \qquad
    \bar{L}_c^{\,\mathrm{ref}} = \frac{1}{N_{\mathrm{ref}}}
    \sum_{k=1}^{N_{\mathrm{ref}}} L_c(t_k).
\end{equation}
%
An independent estimate is obtained from the motor encoders as
$\Delta L_c^{\,\mathrm{enc}}(t) = \bigl(\theta_c(t) - \bar{\theta}_c^{\,\mathrm{ref}}\bigr)\,r_s$, where $\theta_c$ is the measured shaft angle and $r_s = \SI{20}{mm}$ is the spool radius.
The two estimates are compared directly in the time domain; agreement between them constitutes a geometric consistency check, confirming that the encoder records and the motion-capture poses are mutually consistent and that the assumed cable-routing geometry is correct.
%
Since the scope of this section is to show the consistency of the measured cable lenght with the deformation, we provide a comparison with the motion on the $x$ and $y$ plane.
Such comparison is presented in Figure \ref{fig:cable lenght}.
\textbf{Add RMSE}

Consider computing the tendon lenght as straight lines between the disks and not as a smooth path
cite paper Jessica from teaching


\subsection{Force--kinematics consistency}
\label{sec:force_kin_consistency}
%% This is the KEY subsection that justifies using GVS.
%% 
%% FRAMING (important for NSD reviewers):
%%   The cable tensions and base wrench are measured by independent sensors 
%%   (Mark10 and ATI-FT respectively). The kinematic state is measured by 
%%   OptiTrack/FBGS. There is NO way to directly compare a force measurement 
%%   with a kinematic measurement without a physical model that relates them.
%%   Therefore, a dynamics simulation is used strictly as a cross-validation 
%%   tool, not to present new modeling results.
%%
%% Procedure (document clearly):
%%   1. The robot mechanical parameters (E, I, rho, etc.) are characterized 
%%      independently via static loading tests — describe these briefly or 
%%      point to Methods section.
%%   2. The measured cable tensions from the Mark10 sensors are used as INPUT 
%%      to a GVS-based forward dynamics simulation.
%%   3. The simulation OUTPUTS are: (a) the time-varying robot shape (tip 
%%      position at the disk locations), and (b) the reaction wrench at the 
%%      base.
%%   4. These simulated outputs are compared against the independently measured 
%%      OptiTrack tip positions and ATI-FT base wrench.
%%   5. Agreement confirms that the force and kinematic recordings are mutually 
%%      consistent and physically plausible.
%%
%% Important caveats to state:
%%   - The model parameters were obtained from static characterization, not 
%%     identified from the dynamic data. Therefore, perfect quantitative 
%%     agreement is not expected.
%%   - The purpose is to verify that the dataset is internally consistent, 
%%     not to validate the GVS model itself.
%%   - Quantitative model fitting (parameter identification) is left as a 
%%     use case for the dataset.

Cable tensions (measured with Mark10 sensors) and base wrench (measured with ATI-FT sensor) cannot be directly compared against kinematic measurements (OptiTrack, FBGS) without a physical model relating forces to deformations.
To verify that these two families of measurements are mutually consistent, we employ a forward dynamics simulation based on the Geometric Variable Strain (GVS) approach~\cite{Boyer2021} as a cross-validation tool.

As the GVS model requires the parameters of the robot to be known, the next section will address how these parameters were roughtly estimated.
Then, the following section discusses the comparison of the simulation results with the recorded data.

\subsubsection{Rod mechanical parameter characterization}
\label{sec:mech_char}
An equivalent bending rigidity $EI$ of the nitinol backbone was estimated in a set of quasi-static holds, where the robot was commanded to three representative bending modes: planar bending in $xz$, planar bending in $yz$, and a diagonal bending mode produced by co-activating one tendon from each antagonistic pair.
During each hold $w$, inline force gauges provided the tendon tension vector $\boldsymbol{\tau}_w$, while motion capture reconstructed the robot configuration from markers attached to a subset of spacer disks.

To avoid short settling effects after actuation, the first $\sim 200$ samples of each recording were removed and the remaining samples were averaged, yielding disk positions
$\{\mathbf{p}^{\mathrm{meas}}_i(w)\}_{i=1}^{N_{\mathrm{disks}}}$ in the base frame.
For a candidate value of $EI$, a static Cosserat-rod solver was used to predict the equilibrium shape under the measured tensions $\boldsymbol{\tau}_w$ by enforcing the boundary conditions through a root-finding routine.
The simulated disk positions evaluated at the same arc-length locations are denoted by $\mathbf{p}^{\mathrm{sim}}_i(EI,\boldsymbol{\tau}_w)$.

For each bending mode (plane), $EI$ was selected by minimizing the aggregated geometric discrepancy between simulated and measured disk positions over the corresponding set of trials $\mathcal{W}$:
\begin{equation}
\widehat{EI}
=
\arg\min_{EI}
\sum_{w \in \mathcal{W}}
\sum_{i=1}^{N_{\mathrm{disks}}}
\left\|
\mathbf{p}^{\mathrm{sim}}_i(EI,\boldsymbol{\tau}_w) - \mathbf{p}^{\mathrm{meas}}_i(w)
\right\|_2^2 .
\label{eq:EI_ident}
\end{equation}
The resulting plane-wise estimates were $\hat{EI}_{xz}=0.0925$, $\hat{EI}_{yz}=0.08875$, and $\hat{EI}_{\mathrm{diag}}=0.0929~\mathrm{N\cdot m^2}$; a single value $\hat{EI}=0.092~\mathrm{N\cdot m^2}$ was used in the remainder of this work.

Then, the distributed weight of the rod is calculated from the measured weight of the robot ($0.102$ Kg) and the geometry of the rod (length $0.48$ m and radius $2$ mm) producing $\rho = 16910 \; \textbf{kg/m}^3$.

\subsubsection{Comparison of the measured forces}

Using the GVS of \cite{Boyer2021} and the characterized robot materials we have a reliable framework to compute the consistency of the measured values.
Using the tendon tension as input for our simulation, we can compute the corresponding motion of the robot and the forces it exchanges at its base. 
Comparison of the computed and measured torque is presented in Fig. \ref{fig:Base Torque}, were the overall behavior has been captured by the sensor while the simulated spikes are attributed to poorely identified parameters of the robot (\eg{} the distributed weight and corresponding inertia).
%On the other hand, the simulated tip position is shown alongside the measured ones in Fig. \ref{fig:tip trajectory Lissajous_slow}.
% The cable length has been retrieved from the measured rotation angle (relative to the initial home position) and the behavior is consistently shown in Fig. \ref{fig:cable displacement}.

\subsection{Contact force validation}
\label{sec:contact_val}
%% Validate the Resense FT sensor readings using a static equilibrium check.
%% 
%% Procedure:
%%   1. With the robot in a known static configuration (shape measured by 
%%      OptiTrack/FBGS), apply a known force at the tip using a calibrated 
%%      weight or force gauge.
%%   2. The Resense FT sensor at the contact point records this applied force.
%%   3. Using the known shape and applied force, compute the expected wrench 
%%      at the base via static equilibrium (or the GVS static solver).
%%   4. Compare the computed base wrench with the ATI-FT measurement.
%%   5. Agreement validates both the Resense reading and the force 
%%      transmission chain.

To validate the contact force measurements from the Resense FT sensor, we perform a set of static equilibrium checks.
A known external force is applied at the robot tip while the robot is held in a static configuration whose shape is recorded by the OptiTrack system.
Using the measured shape and the applied force, the corresponding reaction wrench at the base is computed via static equilibrium.
This computed wrench is then compared with the direct measurement from the ATI-FT sensor at the base.


% The procedure is as follows.
% The mechanical parameters of the robot (bending stiffness, linear density, cable routing geometry) are characterized independently through static loading tests (see Section~\ref{sec:kin_cross_val}).
% The measured cable tensions from the dataset are then used as the sole input to the GVS forward dynamics simulation.
% The simulation produces two outputs: the predicted time-varying robot shape and the predicted reaction wrench at the base.
% These simulated outputs are compared against the independently recorded OptiTrack measurements (for the shape) and ATI-FT readings (for the base wrench).

% Figure~\ref{fig:gvs_tip} reports the comparison between simulated and measured tip position for a representative circular trajectory.
% Figure~\ref{fig:gvs_wrench} reports the corresponding base wrench comparison.
% %
% % TODO: add figures and RMSE table
% %
% The qualitative and quantitative agreement confirms that the force and kinematic recordings in the dataset are physically consistent.
% We note that, since the model parameters were obtained from static characterization rather than identified from the dynamic data, an exact quantitative match is not expected; residual discrepancies reflect the combined effect of unmodeled dynamics (e.g., material damping, cable friction) and parameter uncertainty.
% Systematic parameter identification using this dataset is left as a downstream use case.


%
% TODO: add figure and discrepancy table

\subsection{Temporal synchronization}
\label{sec:synch_val}
%% This subsection is short but NECESSARY for NSD reviewers: they need to 
%% know how you ensure that data from different sensors can be compared 
%% in time. Since your method is simple (shared wall-clock on one machine), 
%% the subsection can be brief — 1 paragraph + 1 figure.
%%
%% KEY FACTS to state:
%%   - All sensors run as threads in a single Python process on one machine.
%%   - Each sensor thread timestamps its readings with time.time() (POSIX 
%%     wall-clock), so all timestamps share the same OS clock source.
%%   - The synchronization error is bounded by thread scheduling jitter 
%%     (~1-2 ms on the acquisition machine).
%%   - This is adequate given the dynamic bandwidth of interest (<20 Hz, 
%%     i.e., signal periods >50 ms).
%%   - To verify, show a sharp transient event visible across multiple 
%%     sensors and confirm onset alignment.

All sensors are acquired as concurrent threads in a single Python process on a single machine.
Each thread timestamps its readings using the operating system wall-clock (\texttt{time.time()} in Python), ensuring that all sensors share the same clock source without requiring hardware synchronization.
The resulting synchronization error is bounded by the operating system thread scheduling jitter, which is on the order of \SI{1}{\milli\second} to \SI{2}{\milli\second} on the acquisition machine, well below the signal periods of interest (the highest dynamic content in the dataset is below \SI{20}{\hertz}).
%
% TODO: add a figure showing a transient event (e.g., motion onset) across 
% 2-3 sensor streams, confirming that the event onset is aligned within 
% the expected bound.

% \subsection{Repeatability}
% \label{sec:repeatability}
% %% Run the same commanded trajectory multiple times. Overlay the recordings 
% %% and report the inter-trial variability (e.g., std of tip position, std of 
% %% cable tension) to show the data is reproducible.
% %%  
% %% This is a strong argument for dataset quality and is straightforward to 
% %% produce from existing data if you have repeated runs.

% To assess measurement repeatability, the same commanded trajectory is executed in multiple trials.
% %
% % TODO: overlay plots of repeated trials, report inter-trial std


\section{Usage Notes}
%% NSD GUIDANCE: "additional technical notes about how to access or process 
%% the data. Please do not use this section to write a conclusions section, 
%% general selling points, worked case studies, or similar."
%%
%% SUGGESTED CONTENT:
%%   - Software requirements to run the processing code (MATLAB version, 
%%     Python version, required toolboxes/packages).
%%   - How to load and align the raw data (point to process_data.m).
%%   - Known issues: e.g., the FBGS coordinate frame requires rotation 
%%     (the unfinished TODO in process_data.m); any experiments where a 
%%     sensor dropped out or had anomalous readings.
%%   - Note on the effect of IMU mass on the robot inertia (batch 3).
%%   - Guidance on the manual time offset observed in the ATI data (the 
%%     0.22 s offset in test_trajectories.m) — if this is a known issue, 
%%     document it here so users are aware.
%%   - Any limitations on the FBGS reconstruction near the base/tip.
%%

\paragraph{Software requirements.}
The raw and processed data are stored as plain CSV files and can be loaded in any programming language.
The post-processing pipeline (\texttt{process\_data.m}) was developed and tested in MATLAB~R2025b, using only the built-in functions \texttt{butter}, \texttt{filtfilt}, and \texttt{interp1} from the Signal Processing Toolbox.
The GVS simulation code (\texttt{test\_trajectories.m}) additionally requires the MATLAB ODE solvers.
Users who prefer Python can replicate the processing with \texttt{scipy.signal.butter}, \texttt{scipy.signal.filtfilt}, and \texttt{scipy.interpolate.interp1d}, or use \texttt{pandas.read\_csv} to load the data directly.

% \paragraph{Loading raw data.}
% Each raw CSV contains a header row with column names and units.
% Timestamps are recorded as POSIX wall-clock seconds.
% To align multiple sensors in time, subtract a common reference (\eg{} the first motor timestamp) from all timestamp columns and interpolate onto a shared grid, as done in the provided \texttt{process\_data.m} script.

% \paragraph{Loading processed data.}
% The processed CSV files in the \texttt{processed/} subfolder are headerless.
% The first column is always time (in seconds, starting at $t{=}0$ corresponding to the first motor command) and all signals are uniformly sampled at \SI{100}{\hertz}.
% They can be loaded directly via \texttt{numpy.loadtxt} or MATLAB's \texttt{load} function.

\paragraph{Known limitations and caveats.}
\begin{itemize}
    \item \textbf{FBGS coordinate alignment.}
    The FBGS fibre cannot be bonded to the backbone with sub-degree angular accuracy.
    A residual rotational offset between the fibre deformation plane and the robot bending plane is corrected in post-processing using the initial planar calibration sweeps (see Methods).
    Users working with the raw FBGS data must apply this correction themselves.
    \item \textbf{ATI-FT bias.}
    The ATI force/torque sensor exhibits a slowly drifting bias.
    A bias snapshot is recorded at rest before each experiment and subtracted from all readings.
    The unfiltered, bias-subtracted wrench is provided as \texttt{base\_wrench\_raw.csv}; the filtered version is \texttt{base\_wrench.csv}.
    \item \textbf{OptiTrack marker occlusion.}
    Occasional marker occlusions cause invalid frames (\texttt{is\_valid} = 0 in the raw data).
    Users analysing the raw data should filter out invalid frames before further processing.
    \item \textbf{IMU mass (batch~3).}
    In batch~3, four IMU modules (\SI{317}{\milli\gram} each) are mounted on the robot disks.
    Their added mass alters the robot's distributed inertia and may affect dynamic behavior relative to batches~1--2.
    \item \textbf{Motor trajectory generation.}
    The motors are position-controlled; the trajectories are defined as sequences of waypoint angles with a maximum angular velocity limit.
    The actual tip trajectory depends on the robot dynamics and is not a perfect geometric curve.
\end{itemize}

\section{Data Availability}
%% NSD GUIDANCE: For Data Descriptors, repeat the main points from Data Records 
%% here: repository name, DOI, URL. This is a short statement (1 paragraph).
%%
%% NSD accepts: figshare, Zenodo, Dryad, or domain-specific repositories.
%% GitHub alone is NOT sufficient for data — use it for code only.
%% NSD says: "We require that data are public and available in a formal data 
%% repository that meets our policy."
%%
%% TEMPLATE:
%%   The dataset described in this work is available on figshare at 
%%   \url{https://doi.org/10.6084/m9.figshare.XXXXXXX} (Ref.~[N]).
%%   The repository contains all raw and processed sensor recordings 
%%   organized as described in the Data Records section.
%%
Many people in Nature Scientific Data use figshare for which I have created an account.
% NOTE: GitHub is for code. Data must go on figshare (or Zenodo).
% NSD is NOT double-blind — reviewers see author names.

\section{Code Availability}
%% NSD GUIDANCE: "a statement must be included [...] indicating whether and 
%% how custom code can be accessed, including any restrictions to access."
%% Required even if no code is shared.
%%
%% TEMPLATE:
%%   The Python acquisition scripts and MATLAB post-processing and 
%%   technical validation code are available on GitHub at 
%%   \url{https://github.com/ContinuumRoboticsLab/XXXXX} (Ref.~[N]). 
%%   The code is released under the [MIT/BSD/...] license. 
%%   The acquisition code requires Python~3.X with the following packages: 
%%   [list]. The post-processing code requires MATLAB~R202Xa with the 
%%   Signal Processing Toolbox.
%%
Statement about releasing the code for the data acquisition and the postprocessing \& technical validation.

%% NSD GUIDANCE ON REFERENCES:
%% References must be embedded directly in the .tex file as 
%% \begin{thebibliography}{99} ... \end{thebibliography}.
%% Do NOT use a separate .bib file. Remove the \bibliography{bibliography} 
%% command at the end of the document and replace with inline references.
%% Use the Nature referencing style (numbered, author-title-journal).
%% Include DOIs as URLs: https://doi.org/XXXXX
%% Data citations go in the same reference list.

\section{Author Contributions}
%% NSD GUIDANCE: Required. Briefly describe each author's contribution.
%% TEMPLATE:
%%   A.G. conceived the study and designed the experiments. 
%%   T.Z. and A.P. performed the data collection. 
%%   S.T. developed the acquisition software and configured the sensors. 
%%   P.D. designed and fabricated the robot prototype. 
%%   C.S. [contribution]. 
%%   J.B.-K. supervised the project. 
%%   All authors reviewed the manuscript.

\section{Competing Interests}
%% NSD GUIDANCE: Required even if negative.
%% TEMPLATE:
%%   The authors declare no competing interests.
The authors declare no competing interests.

% \section{Acknowledgments (optional)}
%% NSD: Optional. Brief. Do not thank editors. Do not describe funding here.

\section{Funding}
%% NSD GUIDANCE: Required. List all funding sources, organizations, and 
%% grant numbers. If not externally funded, state that.
%%
%% TEMPLATE:
%%   This work was supported by [Agency] under grant [number]. 
%%   J.B.-K. is supported by [grant].

% \section{Ethics statement}
%% Not needed — no human/animal subjects.

    

\bibliography{bibliography}

\end{document}
